//using modulus to see if a number is evenly divisible by two. logging to console using template literals.

for (i = 0; i <= 15; i++) {
    
    if (i % 2 > 0) {
        console.log(`${i} is odd.`)
    }
    else {
        console.log(`${i} is even.`)
    }
}